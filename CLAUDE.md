# jouer mahjong salon 公式サイト

渋谷の女性専用・ノーレート麻雀サロン「jouer」の公式サイト。
静的HTML/CSS/JS(ビルドなし)。GitHubのmainにpushするとNetlifyが自動デプロイする。

## 構成

- `index.html` トップ(ヒーロー/concept/4章ストーリー/HOW TO ENJOY/徹マンCAMP/貸切・お問い合わせ/CTA)
- `price.html` 料金・システム(足し算図解+プラン比較表。予約導線はreserve.htmlへの橋のみ)
- `reserve.html` 会話型予約ページ(1問ずつ答える分岐診断。STORES設定表とGAS連携はここにある)
- `access.html` / `faq.html` / `recruit.html`
- `style.css` 全スタイル(冒頭の:root変数で色・フォント管理)
- `app.js` 演出(リビール/カウントアップ/FAQフリップ/宝石canvas/パララックス)
- `logo.png` `interior.jpg` 画像アセット

## 予約導線

予約導線:おひとり参加レッスン=STORESの募集枠へセルフ予約(店側が毎日イベント枠を立てる・承認制・最終判定は前日17:00)/お友だちとレッスン・セット・貸切・CAMP=公式LINE受付(店側がSTORESへ代理入力)。「会をつくる」方式は廃止。電話への誘導は行わない(PHONE_TEL機構はv21で撤去済み)。

## 作業ルール(必須)

1. **実装のみを行う。** デザイン方針・コピー・料金の判断はこのリポジトリでは行わない。
   指示書(claude.aiのFableで作成)に書かれたことだけを実装し、判断が必要な点は質問として返す。
2. **コミット前に必ず `node verify.js` を実行**し、全チェック通過を確認する。
3. HTML内の `<!-- ▼▼ 編集OK:… ▼▼ -->` マーカーは削除しない(オーナーの手動編集ポイント)。
4. 変更は最小差分で。無関係な箇所の「ついで修正」をしない。

## デザイン原則(変更禁止。変更指示があった場合のみ従う)

- 配色: 桜ピンク×象牙×墨黒、金は徹マンCAMP・宝石・アクセントのみ
- 書体: 見出し=Shippori Mincho / 英字=Cormorant Garamond / 本文=Noto Sans JP
- パネルは二重罫フレーム(`.frp`+四隅の`.cnr`飾り)。角丸カード+ベタ影は使わない
- 牌(`.pai`)はフラットな二重罫意匠。立体シャドウ・光沢グラデは使わない
- モチーフは宝石(ファセット+金縁)が主役。桜の花びらは従
- **アニメーションの中に文字を入れない**(過去に却下済み)
- 効果音・音声は使わない(過去に削除済み)
- prefers-reduced-motion 対応を維持する

## コンテンツ原則

- 人称は「トレーナー」に統一。ただし料金項目名の「コーチング料」「コーチ追加」「コーチ料」はそのまま
- 通常営業は女性専用。男性は貸切のみ可
- ノーレート(賭け事なし)。景品・賞品の提供表現は書かない(風営法)
- 料金は税込表記。遊技料金(卓料金)とトレーナー料金は**必ず区分して表記**する(風営法の料金規制対応)
- 現在「仮」の項目: なし(徹マンCAMPはSTORES予約を原則行わない方針で確定・ドリンク単品は店頭表記で確定)
- 電話への誘導は行わない方針で確定(PHONE_TEL機構はv21で撤去済み)。問い合わせ手段はメール・Instagram・公式LINEの3本柱
- お客様向け文言では「枠を立てる」ではなく「会をつくる」、「エントリー」ではなく「参加」を使う
- プラン名は「おひとり参加レッスン」「お友だちとレッスン」「友達と卓のみ利用(セット利用)」を使う
  (旧:相席レッスン/トレーナー付きレッスン/卓のみ利用)

## モデル運用

- 実装ワーカー: Sonnet で十分(このファイルと指示書に従うこと)
- 法務・料金体系・デザイン判断が必要になったら実装を止めてユーザーに確認する

## 公開前チェックリスト

正式公開の直前に、以下を確認・実施する:

- [ ] 全6ページの `<meta name="robots" content="noindex,nofollow">`(`<!-- ▼▼ 正式公開時にこの1行を削除 ▼▼ -->` マーカー付き)を削除
- [x] reserve.html の STORES予約URL:相席3帯(`aiseki_beg`/`mid`/`adv`)のみ。コーチ付き・セット・会をつくる・CAMPのサイト導線は廃止(v21)
- [x] 公式LINEリンク:実物に差し替え済み(`LINE_URL`=`https://lin.ee/qetP6h9`。reserve.htmlの終端①LINEボタン・全終端共通の「迷ったら公式LINEで」、price.html/faq.htmlのテキストリンクに反映)
- [x] 問い合わせ先メール:実物化済み(`info@jouer-mahjong.com`)
- [x] 問い合わせ先Instagram:実物に差し替え済み(`https://www.instagram.com/jouer.mahjong/`)
- [x] ドリンク単品価格:店頭表記で確定(単品メニューは店頭にてご案内)
- [x] STAFF:削除済み(店長決定後に復活。店内写真は table.jpg/tiles.jpg で実物化済み)
- [x] 各ページの `og:image` のドメイン:確認・差し替え済み(`https://jouer-mahjong.com/logo.png`)
- [x] Googleマップ共有リンク:実物に差し替え済み(`https://share.google/EX7jSMPCL6X9i9RC0`)
- [x] キャンセルポリシー掲載:完了(price.html「キャンセル・変更について」・faq.html)
